[core]
    pager = delta
[pager]
    blame = delta
    format-patch = delta
[interactive]
    diffFilter = delta --color-only --diff-so-fancy

[delta]
    navigate = true
    line-numbers = true
    hyperlinks = true
    features = Dracula
